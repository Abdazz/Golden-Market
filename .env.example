# Copier en .env et remplir (ne jamais committer le vrai .env).
# Variables de docker-compose.yml (infra de dev) — le backend Medusa lit
# apps/backend/.env séparément.

POSTGRES_DB=medusa-backend
POSTGRES_USER=medusa
POSTGRES_PASSWORD=change_me